@model Model.Product

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script src="js/vendor/jquery-1.11.2.min.js"></script>

@using (Html.BeginForm(Html.BeginForm("", "", FormMethod.Post, new { @class = "form-horizontal" })))
{
    @Html.AntiForgeryToken()


    @*<div class="form-horizontal">
            <h2 class="red">Create Product</h2>
            <hr />
            @if (@ViewBag.ResultMessage != null && ((string)@ViewBag.ResultMessage).Contains("Error"))
            {
                <p class="label-warning">@ViewBag.ResultMessage</p>
            }
            else if (@ViewBag.ResultMessage != null)
            {
                <p class="label-success">@ViewBag.ResultMessage</p>
            }
            @Html.ValidationSummary(true)
            <div class="form-group">
                @Html.LabelFor(model => model.ProductName, new { @class = "control-label col-sm-3" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.ProductName)
                    @Html.ValidationMessageFor(model => model.ProductName)
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.PID, new { @class = "control-label col-sm-3" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.PID)
                    @Html.ValidationMessageFor(model => model.PID)
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.ProductDescription, new { @class = "control-label col-sm-3" })
                <div class="col-md-10">
                    @Html.TextAreaFor(model => model.ProductDescription, new { rows = "6", cols = "50" })
                    @Html.ValidationMessageFor(model => model.ProductDescription)
                </div>
            </div>
            <p>
                <input id="btnCreateProduct" type="submit" value="Create" />
                <input id="btnResetProduct" type="Button" value="Reset" class="cancel" />
            </p>
        </div>*@


    <div class="panel panel-default">
        <div class="panel-heading">Create Product</div>
        <div class="panel-body">

            <div class="form-group">
                @Html.LabelFor(model => model.ProductName, new { @class = "control-label col-sm-3" })
                <div class="col-sm-9">
                    @Html.TextBoxFor(model => model.ProductName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.ProductName)
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.PID, new { @class = "control-label col-sm-3" })
                <div class="col-sm-9">
                    @Html.TextBoxFor(model => model.PID, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.PID)
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.ProductDescription, new { @class = "control-label col-sm-3" })
                <div class="col-sm-9">
                    @Html.TextAreaFor(model => model.ProductDescription, new { rows = "6", cols = "50", @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.ProductDescription)
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-12 text-center">
                    <button id="btnCreateProduct" type="submit" class="btn btn-default" value="Create">Create</button>
                    <button id="btnResetProduct" type="button" class="btn btn-default" value="Reset">Reset</button>
                </div>
            </div>

        </div>
    </div>
}
@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            $("#btnResetProduct").click(function () {
                $('input[type="text"]').val('');
                $('#ProductDescription').val('');
                $(".validation-summary-errors").css("display", "none");
                $(".input-validation-error").removeClass("input-validation-error");
                $(".field-validation-error").children().remove();
                $("P.label-success").remove();
                $("P.label-warning").remove();
            });


        });
    </script>
}
